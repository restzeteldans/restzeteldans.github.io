
  # Municipality Election Guide

  This is a code bundle for Municipality Election Guide. The original project is available at https://www.figma.com/design/dBDBIA746awRwec47Ko5P1/Municipality-Election-Guide.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  